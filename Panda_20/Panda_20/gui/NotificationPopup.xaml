<Window x:Class="Panda_20.gui.NotificationPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="Manual"
        ResizeMode="NoResize"
        Title="Notification" Height="170" Width="350"
        WindowStyle="None"  Background="Transparent"  AllowsTransparency="True" >

    <Border BorderBrush="LightGray"  BorderThickness="3,3,3,3" CornerRadius="8,8,8,8" Opacity="0.8" >
                  
        <Grid Background="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="20" Name="TopBar"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Name="DismissButton" Grid.Row="0"  Height="16" Width="16" Grid.Column="1" Margin="252,2,2,2" Click="DismissButton_OnClick">
                <Image Name="DismissImage"></Image>
            </Button>
            <Button Name="FacebookButton" Grid.Row="0"  Height="16" Width="16" Grid.Column="1" Margin="230,2,22,2" Click="FacebookButton_OnClick">
                <TextBlock>f</TextBlock>
            </Button>


            <Image Name="FbPicture" Width="50" Height="50" Grid.Row="1" Grid.Column="0" Margin="10,10,11,102"/>
            <TextBlock Width ="260" Grid.Row="1" Grid.Column="1" Margin="9,10,10,30" Background="White"/>

            </Grid>
    </Border>

</Window>

